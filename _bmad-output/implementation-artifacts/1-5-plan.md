# Story 1.5 Plan

1. Extend Auth.js config with GitHub + Google providers, preserve credentials flow, and enable trusted email-based account linking.
2. Add reusable OAuth buttons + login error banner; render both on /login and /register without regressing existing forms.
3. Extend env validation + .env.example for OAuth credentials.
4. Add unit coverage for OAuth button behavior, login error rendering, and provider registration.
5. Run lint + test; save dev-story artifact; complete workflow.
